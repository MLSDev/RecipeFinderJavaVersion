<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModel"
            type="com.mlsdev.recipefinder.view.analysenutrition.recipe.IngredientsAdapter.AddIngredientViewModel" />
    </data>

    <android.support.v7.widget.AppCompatButton xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/btn_add_ingredient"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/activity_vertical_margin"
        android:background="@drawable/tab_item_background"
        android:onClick="@{viewModel.onAddIngredientButtonClick}"
        android:text="@string/btn_add"
        android:textColor="@android:color/white" />

</layout>